# Pintos 대비 진단 테스트

> 반영 사항: 네가 말한 것처럼 **KAIST x86-64 기반 Pintos에서 “파일 시스템 구현(Project 4)”은 안 한다**는 전제하에, **파일시스템 구현 문제는 제외**했어.  
> 다만 Project 2에서의 **파일 관련 시스템콜/FD 테이블/동시성 이슈**는 그대로 중요하니 그쪽은 포함했어.

## 안내

- 목적: **Pintos(Threads / User Programs / Virtual Memory)에서 막히는 지점을 수치화**해서, 이후에 “뭘 준비해야 ROI가 큰지”를 정확히 잡기 위함.
- 권장 소요 시간: 60–90분  
- 규칙(권장): **검색/책 없이** 먼저 풀어봐. 모르겠으면 “모르겠음 + 추측”도 적어줘(진단 정확도가 올라감).
- 빠른 버전(30분): 각 섹션의 **(★)** 문제만 먼저.

## 문제

### C / 포인터 / 메모리 안전

**Q1 (★, 6점)** 다음 코드의 출력/동작을 **정확히** 설명해줘. UB(정의되지 않은 동작)가 있으면 “UB”라고 쓰고, 왜 UB인지 적어줘.

```c
#include <stdio.h>

int main(void) {
    int a[3] = {10, 20, 30};
    int *p = a;

    printf("%d\n", *p++);
    printf("%d\n", *++p);
    printf("%d\n", *(p + 0));
    return 0;
}
```
(0=찍음/추측, 1=애매, 2=확신).
Q1: p에는 배열의 처음 주소값 즉, 첫번째 요소의 주소값이 들어가기 때문에 첫번째 출력은 후위 증가 연산자에 의해 10이 나온다. 이후에는 이미 다음 요소의 주소값으로 후위 증가 연산자가 적용된 상태이기 때문에 전위 증가 연산자를 적용한 결과인 30이 출력된다./ 자신감: 1

**Q2 (★, 6점)** 다음 코드가 출력하는 값(또는 UB 여부)을 답하고, 이유를 적어줘.

```c
#include <stdio.h>
#include <string.h>

int main(void) {
    char s[4];
    strcpy(s, "ABCD");
    printf("%zu %s\n", strlen(s), s);
    return 0;
}
```
(0=찍음/추측, 1=애매, 2=확신).
Q2: 모르겠음. 널값 없어서 에러 날 것 같다. %zu도 뭔지 모르겠음 / 자신감: 0

**Q3 (6점)** 다음 구조체의 `sizeof(struct S)`가 64-bit 리눅스(gcc, 일반적인 정렬 규칙)에서 얼마일지 추정하고, **패딩이 어디 들어가는지** 설명해줘.

```c
struct S {
    char  c;
    int   x;
    short y;
    char  d;
};
```
(0=찍음/추측, 1=애매, 2=확신).
Q4: char는 1바이트가 고정일 것 같고 64비트에서 int는 8바이트이기 때문에 short도 4바이트 정도 되지 않을까 싶다. char는 1바이트이므로 다 합치면 14바이트인데 보통 빈 공간을 패딩해줘야 하기 때문에 이진수로 나눠 떨어지는 16바이트까지 맨 마지막에 패딩해줄 것 같다./ 자신감: 0

**Q4 (★, 6점)** 아래 함수는 “배열 길이를 구하는 함수”처럼 보이지만 실제로는 문제가 있다.  
문제가 무엇인지, 고치려면 어떤 인터페이스가 되어야 하는지 설명해줘.

```c
#include <stddef.h>

size_t len(int a[]) {
    return sizeof(a) / sizeof(a[0]);
}
```
(0=찍음/추측, 1=애매, 2=확신).
Q4: 모르겠다. 아마 a[0]로 접근하는 게 아니라 a[0]의 type의 size를 해야 할 것 같다. / 자신감: 0

**Q5 (6점)** 다음 코드는 의도는 “힙에 int 하나를 만들고 7을 저장”이야.  
문제점을 찾고, 수정된 코드를 제시해줘.

```c
#include <stdlib.h>

int main(void) {
    int *p = malloc(sizeof(p));
    *p = 7;
    free(p);
    return 0;
}
```
(0=찍음/추측, 1=애매, 2=확신).
Q5: p가 정의되지 않았는데 sizeof()를 쓰고 있다. p를 먼저 선언하고 나서 사용해야 한다. 코드는 다음과 같을 것이다.
```c
#include <stdlib.h>

int main(void) {
    int p;
    int *p = malloc(sizeof(p));
    *p = 7;
    free(p);
    return 0;
}
```

/ 자신감: 1

**Q6 (4점)** “포인터/메모리 관련 버그로 크래시가 나는 전형적 원인 3가지”를 예시와 함께 적어줘.  
(예: NULL 역참조, 해제 후 사용(UAF), 범위 밖 접근(OOB), double free 등)
(0=찍음/추측, 1=애매, 2=확신).

Q6: UAF는 메모리를 해제한 이후에 사용하려고 접근하는 것이고 NULL 역참조는 모르겠고 OOB도 모르겠고 double free는 잘 모르지만 이미 free한 메모리를 또 free 해주는 문제일 것 같다. / 자신감: 0

---

### 빌드 / 디버깅(기초 체력)

**Q7 (★, 6점)** 다음 컴파일 옵션들이 각각 어떤 의미/효과가 있는지 간단히 설명해줘.  
`-g`, `-O0`, `-O2`, `-Wall`, `-Wextra`, `-Werror`
(0=찍음/추측, 1=애매, 2=확신).
Q7: -g는 모르겠고 -O0는 최적화 레벨은 0단계로 설정하겠다는 것으로 알고 있고 -O2는 최적화를 2단계로 적용하겠다는 것으로 알고 있다. 나머지는 모르겠다. / 자신감: 0

**Q8 (4점)** 아래 gdb 백트레이스가 있을 때, 네가 다음으로 확인할  
1) “가장 먼저 볼 것 3가지”  
2) “그 이유”  
를 적어줘.

```
#0  0x0000000000401146 in foo (p=0x0) at main.c:12
#1  0x000000000040117a in bar () at main.c:21
#2  0x00000000004011a0 in main () at main.c:28
```

(0=찍음/추측, 1=애매, 2=확신).
Q8: 1) foo의 내용 bar의 내용. main의 내용 2) 위에서부터 가장 오류와 같은 레벨에 있는 함수일 것이기 때문이다. / 자신감: 0

---

### 프로세스 / 스레드 / 시스템콜(경계 감각)

**Q9 (★, 6점)** “프로세스”와 “스레드”의 차이를 **주소공간/자원 공유 관점**에서 정리해줘.  
아래 항목이 각각 “프로세스 단위 vs 스레드 단위” 중 어디에 더 가까운지도 표시해줘.
- 가상 주소 공간
- 파일 디스크립터 테이블(일반적인 OS 기준)
- 스택
- 레지스터 집합
- 힙
- 동기화(락/세마포어) 객체의 ‘소유’ 개념(대략)

(0=찍음/추측, 1=애매, 2=확신)
Q9: 프로세스 간에는 주소공간과 자원을 기본적으로는 공유하지 않는다. IPC 혹은 공유 메모리와 같은 별도의 기능을 사용해야 자원을 공유할 수 있다. 그러나, 같은 프로세스에서 만들어진 스레드의 경우 프로세스에 있는 스택을 공유해서 사용할 수 있기 때문에 차이가 있다. 매번 무겁게 새로운 프로세스를 생성할 필요 없이 동시성을 지원할 수 있게 되는 것에 더해 공유 자원까지 사용할 수 있게 되는 것이다. 가상 주소 공간은 프로세스 단위에 가깝고 파일 디스크립터 테이블은 일반적으로 프로세스에 가깝다. 스택도 기본적으로는 프로세스에 할당되며 레지스터 집합은 보기에 따라 다르지만 프로세스에 할당된 레지스터를 스레드마다 각각 쓸 수 있다고 생각할 수 있다. 힙의 경우에는 프로세스에 가깝고 동기화와 객체의 소유 개념에서는 대략적으로 스레드에 가깝다. / 자신감: 1

**Q10 (★, 6점)** 유저 프로그램이 시스템콜을 호출할 때(예: `read`, `write`)  
**유저 모드 → 커널 모드 → 유저 모드**로 돌아오는 “대표 흐름”을 단계로 써줘.  
(정확한 CPU 세부는 몰라도 되지만, *어떤 정보가 어디로 전달/검증*되는지가 핵심)
(0=찍음/추측, 1=애매, 2=확신).
Q10: 유저 모드에서 시스템콜 호출하면 인터럽트 발생 그러면 OS의 인터럽트 핸들러가 해당 인터럽트에 대한 처리를 수행. 그 결과를 유저 모드에 반환한다. / 자신감: 1

**Q11 (4점)** 커널에서 유저 포인터(사용자 메모리 주소)를 받았을 때, 왜 “그냥 역참조하면 위험”한지 설명하고, 안전하게 처리하기 위한 원칙을 3가지 적어줘.
(예: 검증, 예외처리, 접근 방식 등)
(0=찍음/추측, 1=애매, 2=확신).
Q1: 악의적인 의도 혹은 실수로 인해서 참조하면 안 되는 영역을 참조할 수 있기 때문이다. / 자신감: 0

---

### 동기화 / 스케줄링(Phase 1 핵심)

**Q12 (★, 8점)** 아래 코드는 멀티스레드에서 문제가 생긴다.  
1) 어떤 문제가 생길 수 있는지(가능한 결과)  
2) 최소 수정으로 어떻게 고칠지(락을 어디에 둘지)
3) 락을 너무 크게 잡을 때 생길 수 있는 부작용(성능/데드락/우선순위 등)  
을 적어줘.

```c
int shared = 0;

void inc1000(void) {
    for (int i = 0; i < 1000; i++) {
        shared++;
    }
}
```
(0=찍음/추측, 1=애매, 2=확신).
Q12: 1) shared가 1000까지 증가하지 않을 수 있다.  2) shared를 증가시키는 부분에 락을 두면 된다. 3) 락을 너무 크게 잡으면 다른 스레드의 작업에 병목을 일으킬 수 있을 것이다. / 자신감: 1

**Q13 (6점)** 데드락이 성립하기 위한 대표 “4조건”을 적고, OS 프로젝트에서 현실적으로 쓰는 회피/완화 전략 2가지를 설명해줘.  
(예: 락 순서 규칙 같은 것)
(0=찍음/추측, 1=애매, 2=확신).
Q13: 모르겠다. 추측하자면 다음과 같다. 상호 배제, 기다림, 공유 자원, 순서의 존재. 자원을 공유하는 것이 아니라 상태를 공유한다. 나머지 하나는 모르겠다. / 자신감: 1

**Q14 (★, 6점)** “우선순위 역전(priority inversion)”이 무엇인지 설명하고, 이를 완화하는 기법(예: donation)의 핵심 아이디어를 짧게 적어줘.  
가능하면 간단한 시나리오(스레드 A/B/C 우선순위 포함)도 같이.
(0=찍음/추측, 1=애매, 2=확신).
Q14: 우선순위가 비교적 낮은 스레드가 락을 가지고 있는데 그 락을 필요로 하는 비교적 우선순위가 높은 스레드가 락이 없어서 작업을 못하는 상황이 아닐까 싶다. 선점형 스케줄링과 연관 있지 않을까 싶은데 잘 모르겠다. / 자신감: 0

---

### 가상 메모리 / Malloc 감각(Phase 2~3 연결)

**Q15 (★, 8점)** “페이지 폴트(page fault)”가 났을 때 OS가 하는 일을 가능한 한 구체적으로 단계로 써줘.  
핵심 포인트:
- 폴트의 원인 분류(대표 케이스들)
- 어떤 자료구조/메타데이터를 보고 판단하는지
- 무엇을 적재/매핑/갱신하는지
(0=찍음/추측, 1=애매, 2=확신)
Q15: Not in memory인 경우와 Page Fault인 경우 두 가지로 나뉜다. Page에는 있지만 로딩된 적이 없어서 아직 Memory에 없는 경우에는 로딩만 하면 되지만 애초에 접근할 수 없는 페이지인 경우에는 SegFault 등의 에러를 통해서 Interrupt를 발생시킨다. 비트 단위로 표시되는 valid bit 등의 메타 데이터를 보고 판단한다. SIGSEG 같은 시그널도 사용될 것이다. 무엇을 적재/매핑/갱신하는지는 잘 모르겠다. Storage의 데이터를 Memory에 적재하여 매핑하며 최신 자료로 갱신하는 것이라고 생각한다. / 자신감: 1

**Q16 (8점)** 너라면 Malloc Lab 수준에서 아래 요구를 만족하는 `malloc/free`를 어떻게 설계할지 설명해줘.  
(코드 전체가 아니라 **자료구조 + 알고리즘 + 불변식** 중심)

요구:
- 16바이트 정렬
- free block coalescing(인접 free 합치기)
- splitting(큰 free를 쪼개 할당)
- 성능 고려 1개 이상(예: first-fit/next-fit/segregated list 중 택1)

답안에 꼭 포함:
- 블록 메타데이터 형식(헤더/푸터에 무엇을 넣을지)
- free list(implicit/explicit) 선택 및 이유
- `malloc` 흐름(탐색→분할→표시)
- `free` 흐름(표시→병합→리스트 연결)
- 깨지면 바로 터지는 불변식 2개(예: 정렬, 경계 태그 일치 등)

(0=찍음/추측, 1=애매, 2=확신).
Q16: 모르겠다. free는 실제로 free하는 것이 아니라 free list에 해당 메모리 주소를 기록하는 것으로 알고 있다. 나머지는 잘 모르겠다. first-fit을 쓰고 싶다. first-fit을 해도 성능상 큰 영향이 없고 잘 동작하는 것으로 알고 있다. free인 곳을 찾아서 적당한 크기로 자른 뒤 반환해서 표시한다. free는 현재 free 되었다고 표시하고 free끼리 합친 뒤 free 메모리 리스트에 올려서 관리한다. / 자신감: 0

**Q17 (4점)** 아래 상황에서 **외부 단편화(external fragmentation)**가 커지는 이유를 짧게 설명하고, 이를 줄이는 접근 2가지를 적어줘.  
상황: “작은 블록을 많이 할당/해제하며, 서로 떨어진 free 조각이 많이 생기는 워크로드”
(0=찍음/추측, 1=애매, 2=확신).
Q17: 작은 블록을 많이 할당하고 해제하게 되면 비교적 큰 블록을 할당할 수 없게 된다. 서로 떨어진 free 조각이 많이 생기는 경우에도 그 조각보다 작은 블록밖에 할당할 수가 없게 된다. 따라서 아예 page의 크기를 일정하게 유지하는 편이 낫다. 나머지 한 개의 방법은 잘 모르겠다. / 자신감: 1


---

## 제출 형식(복붙해서 답해줘)

아래 템플릿 그대로 채워줘.  
각 문항 끝에 **자신감(0~2)**도 같이 적어줘(0=찍음/추측, 1=애매, 2=확신).

- Q1: (답) / 자신감:
- Q2: (답) / 자신감:
- …
- Q17: (답) / 자신감:

추가로, 가능하면 마지막 줄에 이것도 적어줘:
- “지금까지 C로 포인터/자료구조 직접 구현한 경험(예: 연결리스트/트리)”: 예/아니오 + 짧게
- “gdb 사용 경험”: 예/아니오 + 짧게
- “동시성(락/세마포어) 직접 다뤄본 경험”: 예/아니오 + 짧게

---

## 채점 루브릭(내가 채점할 기준)

총점 100점.

- C/포인터/메모리 안전: 34점 (Q1~Q6)
  - **정답 + 이유/근거가 정확**하면 만점
  - “대충 감”만 있으면 부분점수(절반 이하)
  - UB를 UB로 못 잡으면 큰 감점
- 빌드/디버깅: 10점 (Q7~Q8)
  - 옵션 의미를 단순 나열이 아니라 “왜 쓰는지(디버그/성능/경고 정책)”까지 말하면 고득점
- 프로세스/스레드/시스템콜: 16점 (Q9~Q11)
  - “경계(유저/커널, 주소공간, 검증)”를 정확히 이해하면 고득점
- 동기화/스케줄링: 20점 (Q12~Q14)
  - race를 결과로 설명 + 정확한 락 위치 + 우선순위 역전 개념까지 연결하면 고득점
- VM/Malloc: 20점 (Q15~Q17)
  - “원인→판단→조치(매핑/적재/갱신)” 흐름이 있으면 고득점
  - malloc은 자료구조/불변식이 없으면 감점

---

## 점수 해석 가이드(다음 단계에서 ROI를 결정할 구간)

- **80–100점:** 바로 Pintos 들어가도 됨. (부족한 영역만 핀포인트 보강)
- **65–79점:** “돌리면서 배우는” 건 가능하지만, 특정 영역이 발목 잡을 확률 큼 → **약점 영역 집중 보강**이 ROI 최고
- **50–64점:** Pintos 시작은 가능하나, 디버깅/동기화/유저포인터 검증에서 시간을 크게 잡아먹을 가능성 높음
- **0–49점:** 지금 들어가면 프로젝트가 “OS가 아니라 C 디버깅”이 될 확률 큼 → **C/메모리/디버깅을 먼저** 잡는 게 ROI 최댓값

영역별 위험 신호(대략):
- C 영역이 **24/34 미만**: 포인터/UB/메모리 안정성부터 보강하지 않으면 Pintos가 매우 비싸짐
- 동기화가 **12/20 미만**: Phase 1에서 donation/락 설계에서 크게 흔들릴 확률 높음
- 시스템콜/유저포인터가 **10/16 미만**: Phase 2에서 “왜 자꾸 죽지?”가 반복될 확률 높음
- VM/Malloc이 **12/20 미만**: Phase 3에서 page fault/mmap/swap 개념이 계속 헷갈릴 확률 높음

---

네 답안을 보내주면, 내가 **점수 + 영역별 진단 + “Malloc Lab까지 가능한” 최우선 보강 로드맵(ROI 기준)**까지 바로 이어서 만들어줄게.
